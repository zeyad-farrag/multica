# PLAN — TIM-2: Story 1.2 — Multica fork: add `estimate_minutes` to issues (M-PR#1)

Architect: Winston (`bmad-agent-architect`).
Worktree branch: `multica/tim-2`. Base: `origin/bmad-self-host` (current HEAD `ac5902b4`).
Per push rule, this branch lives **locally only** until Marcus pushes at `in_review`. The earlier `origin/multica/tim-2` deletion (and PR #17 closure) reflects cleanup of a pre-marker-contract attempt; no recovery needed — the local worktree is the source of truth.

## Goal

Add a nullable `estimate_minutes INT` column to Multica's `issue` table with a positive-value `CHECK`, a partial index `idx_issue_assignee_open` for open-bucket assignee lookups, and surface the field through `PUT /api/issues/{id}` (set / clear / preserve via the existing `rawFields` PATCH semantics) and `GET /api/issues/{id}` (with `computed_estimate_minutes` rolled up via a recursive CTE over leaf descendants whose `assignee_type IS NULL OR = 'member'`). Schema-only change to the Multica fork — no UI surfacing in v1; rollup is single-issue (not list paths).

## Files to touch (M-PR#1 scope)

| Path | Change |
|---|---|
| `server/migrations/<NNN>_issue_estimate_minutes.up.sql` | new |
| `server/migrations/<NNN>_issue_estimate_minutes.down.sql` | new |
| `server/pkg/db/queries/issue.sql` | edit |
| `server/pkg/db/generated/issue.sql.go` | regenerated by `make sqlc` |
| `server/pkg/db/generated/models.go` | regenerated by `make sqlc` |
| `server/internal/handler/issue.go` | edit |
| `server/internal/handler/handler_test.go` | edit (six new test cases) |

Anything else is out of scope for M-PR#1. Do not touch `events.go`, daemon, search, autopilot, batch update, frontend.

## Migration prefix

Pick at PR-open time, not now. Current `origin/bmad-self-host` migration tail (verified this dispatch):

```
056_audit_newly_reserved_slugs
057_feedback
058_drop_autopilot_priority_and_project_id   <-- highest 0xx
101_issue_label_polish                        <-- highest 1xx (≤102 range)
999_bmad_phase_state
1000_bmad_ready_for_dev_and_comment_types
1001_coderabbit_integration
1002_issue_links / 1002_remove_checkpoint    <-- pre-existing duplicate, not ours
1003_issue_review_thread                      <-- highest 1xxx
```

Next free 0xx is **`059`**; next free 1xx is **`102`**. Pick the range that matches the prevailing convention at PR-open time and document the basis in the PR body. Do **not** hard-code `057`.

## Approach

Numbered, top-down. Amelia executes in order.

1. **Re-confirm migration prefix.** `git fetch origin bmad-self-host` then `git log --oneline -- server/migrations/ | head` against `origin/bmad-self-host`. If `059` is still free, use `059`. If a competing branch landed `059` since this plan was written, bump to the next free slot.

2. **Write `<NNN>_issue_estimate_minutes.up.sql`.** One transaction:
   ```sql
   BEGIN;
   ALTER TABLE issue
       ADD COLUMN estimate_minutes INT NULL
       CHECK (estimate_minutes IS NULL OR estimate_minutes > 0);
   CREATE INDEX idx_issue_assignee_open
       ON issue (assignee_type, assignee_id, status)
       WHERE status IN ('todo','in_progress','planning','ready_for_dev','fixing','testing');
   COMMIT;
   ```
   Both DDLs in one file. No `UNIQUE (id, workspace_id)` constraint.

3. **Write `<NNN>_issue_estimate_minutes.down.sql`.** Strict inverse:
   ```sql
   BEGIN;
   DROP INDEX IF EXISTS idx_issue_assignee_open;
   ALTER TABLE issue DROP COLUMN IF EXISTS estimate_minutes;
   COMMIT;
   ```
   Verify with `make migrate-up && make migrate-down && make migrate-up` against a populated dev DB.

4. **Edit `server/pkg/db/queries/issue.sql`.**
   - Append `estimate_minutes` to the explicit column lists in `ListIssues` and `ListOpenIssues`. The `SELECT *` queries (`GetIssue`, `GetIssueInWorkspace`, `GetIssueByNumber`, `ListChildIssues`) and `RETURNING *` queries (`CreateIssue`, `CreateIssueWithOrigin`, `UpdateIssue`) pick up the new column automatically — verify regenerated output.
   - In `CreateIssue` and `CreateIssueWithOrigin`: append `estimate_minutes` to `INSERT (...)` and `sqlc.narg('estimate_minutes')` to `VALUES (...)` (mixing positional and narg is already established by `CreateIssueWithOrigin`'s `origin_type` / `origin_id` lines).
   - In `UpdateIssue`: add `estimate_minutes = sqlc.narg('estimate_minutes'),` next to `assignee_id` / `due_date` / `parent_issue_id` / `project_id`. **Bare `sqlc.narg`, NOT `COALESCE`**. The story's "phase_state precedent" reference is a citation slip — `phase_state` uses `COALESCE` because it intentionally cannot be cleared via the API. `estimate_minutes` MUST be clearable, so it follows the bare-narg group (`assignee_id`, `due_date`, `parent_issue_id`, `project_id`). Handler-side pre-fill from `prevIssue` distinguishes "key absent" (preserve) from "key=null" (clear).
   - Add `ComputeIssueEstimateRollup :one` at the bottom of `issue.sql`. Recursive CTE over `issue` (Multica's source table — NOT `mirror_issue`):
     - Anchor: direct children of `sqlc.arg('issue_id')` filtered by `workspace_id = sqlc.arg('workspace_id')`.
     - Recursion: descendants joined on `parent_issue_id`, scoped to `workspace_id`.
     - Leaves: descendants with no children of their own (`NOT EXISTS (SELECT 1 FROM descendants c WHERE c.parent_issue_id = d.id)`).
     - Sum: `COALESCE(SUM(estimate_minutes), 0)::int` over leaves where `estimate_minutes IS NOT NULL` AND `(assignee_type IS NULL OR assignee_type = 'member')`. Excludes agent leaves (D22/B4). Root's own estimate excluded by construction (anchor starts at children, D37).
   - Run `make sqlc` from project root. Confirm `Issue.EstimateMinutes pgtype.Int4` lands in `models.go` and `ComputeIssueEstimateRollup` lands as a method on `Queries` in `issue.sql.go`.

5. **Edit `server/internal/handler/issue.go`.**
   - Add `EstimateMinutes *int32 \`json:"estimate_minutes"\`` and `ComputedEstimateMinutes int32 \`json:"computed_estimate_minutes"\`` to `IssueResponse`. Pointer for nullable, plain int (always present, default `0`) for the computed rollup.
   - Add an `int4ToPtr(v pgtype.Int4) *int32` helper next to existing `textToPtr` / `uuidToPtr` / `timestampToPtr`. Reuse if one already exists.
   - Populate `EstimateMinutes` in `issueToResponse`, `issueListRowToResponse`, and `openIssueRowToResponse` from `i.EstimateMinutes`.
   - In `GetIssue`, after building `resp`, call `h.Queries.ComputeIssueEstimateRollup(ctx, ComputeIssueEstimateRollupParams{IssueID: ..., WorkspaceID: ...})` and assign to `resp.ComputedEstimateMinutes`. On error: `slog.Warn` with the issue ID and default to `0` — never fail the GET on a rollup error.
   - Do NOT call the rollup on `ListIssues` / `ListOpenIssues` / `ListChildIssues` — single-issue computation only (perf, AC8).
   - Extend `UpdateIssueRequest` with `EstimateMinutes *int32 \`json:"estimate_minutes"\``.
   - In `UpdateIssue`, mirror the existing `rawFields` "explicit-null vs absent" branches (read the `assignee_id` / `due_date` / `parent_issue_id` / `project_id` blocks end-to-end first):
     - Pre-fill: add `EstimateMinutes: prevIssue.EstimateMinutes` to the `params := db.UpdateIssueParams{...}` initializer so absent key preserves existing.
     - If `_, ok := rawFields["estimate_minutes"]; ok`:
       - `req.EstimateMinutes == nil` → `params.EstimateMinutes = pgtype.Int4{Valid: false}` (clear).
       - `*req.EstimateMinutes <= 0` → `writeError(w, http.StatusBadRequest, "estimate_minutes must be > 0")` and return. Validate BEFORE sqlc invocation; the DB CHECK is the last line of defence.
       - `*req.EstimateMinutes > 0` → `params.EstimateMinutes = pgtype.Int4{Int32: *req.EstimateMinutes, Valid: true}` (set).
   - For `CreateIssue` / `CreateIssueWithOrigin` handlers: pass `pgtype.Int4{Valid: false}` to the new sqlc parameter. AC5 only requires PUT acceptance; v1 product flow sets estimates post-creation. Do **not** expand `CreateIssueRequest` — that widens M-PR#1 surface beyond the spec.

6. **Add Go handler tests in `server/internal/handler/handler_test.go`.** Six new cases next to `TestIssueCRUD`, reusing existing scaffolding (`testHandler`, `newRequest`, `withURLParam`, `httptest.NewRecorder()`). Decode response into `IssueResponse` and assert.
   - (a) PUT `{estimate_minutes: 60}` → GET → `*estimate_minutes == 60`.
   - (b) After (a), PUT `{estimate_minutes: null}` → GET → `estimate_minutes == nil`.
   - (c) PUT `{estimate_minutes: 0}` → 400.
   - (d) PUT `{estimate_minutes: -10}` → 400.
   - (e) Parent + two member-assigned children (estimates 30, 45) + one agent-assigned child (estimate 999). GET parent → `computed_estimate_minutes == 75` (agent leaf excluded).
   - (f) Leaf issue with `estimate_minutes: 60`, no children. GET → `computed_estimate_minutes == 0` (own estimate not folded in when no descendants).

7. **Verify partial index is partial (AC10).** On a populated dev DB:
   - `EXPLAIN ANALYZE SELECT id FROM issue WHERE assignee_type = 'member' AND assignee_id = '<uuid>' AND status = 'in_progress';` MUST pick `idx_issue_assignee_open`.
   - Same query with `status = 'done'` MUST NOT use it (sequential scan or different index). Capture both EXPLAIN outputs for the PR description.

8. **Run `make check` from project root (AC8).** `pnpm typecheck` + `pnpm test` + `make test` + `pnpm exec playwright test` all green. If `pnpm typecheck` complains about the new response fields, do an additive bump to the frontend `Issue` type — v1 UI does not surface the field, so do not surface it in the UI.

## Test strategy

- **Unit (Go):** six new cases in `server/internal/handler/handler_test.go` (set / clear / reject-zero / reject-negative / parent rollup with mixed assignees / leaf rollup zero). In-process `testHandler`; no new harness.
- **Integration:** `make migrate-up && make migrate-down && make migrate-up` round-trip against populated dev DB confirms schema reversibility (AC2 / AC3).
- **Manual:** two `EXPLAIN ANALYZE` queries confirming partial index fires for open-bucket statuses and not for `status = 'done'` (AC10). Capture outputs for the PR body.
- **Full pipeline:** `make check` from repo root green (AC8) — `pnpm typecheck`, `pnpm test`, `make test`, `pnpm exec playwright test`.

## Assumptions and how this replan closes Amelia's gap

**Amelia's bounce reason** (`<!-- plan-issue v1 -->` 2026-05-01 22:22 UTC): `origin/multica/tim-2` was deleted by `git fetch --prune`; `gh api .../branches/multica/tim-2` returns 404; PR #17 is CLOSED unmerged.

**How this replan closes the gap:**

1. **Origin branch absence is expected, not broken.** Per the marker-only contract (CLAUDE.md push rule), `multica/<issueBranchKey>` is created and pushed by Marcus only at `in_review`, only after Murat's GREEN. Until then, the branch lives **locally only** in this shared worktree. Amelia, Quinn, and Felix all coordinate on the local branch. The `gh api .../branches/multica/tim-2 → 404` is the correct steady state for any pre-`in_review` story. PR #17 was an artefact of a pre-marker-contract push that violated the current contract; its closure and the upstream branch deletion were correct cleanup, not a corruption signal.

2. **Worktree was contaminated by stale state.** The local `multica/tim-2` HEAD was `4182644d`, based on `db5f1885` — four commits behind current `origin/bmad-self-host` (`ac5902b4`). The drift surfaced as a 5-file phantom diff (`.github/workflows/ci.yml`, `scripts/check_team_app_events.sh`, `server/internal/handler/comment_emit_test.go`, `server/internal/handler/workspace_emit_test.go`, `server/pkg/protocol/events.go`) — files reverted upstream by `ac5902b4` and `251b4b54` but still present in the local tree. Plus three stale architect-authored commits (`4182644d`, `ea5127fe`, `e9610dd0`), the middle of which contains M-PR#1 implementation work that violates Winston's "no production code" rule.

3. **What I did in this dispatch:** `git reset --hard origin/bmad-self-host` on `multica/tim-2`, dropping the three contaminated commits and the phantom-diff files. Then committed a single architect scaffold (`PLAN.md`) on top. The worktree is now clean: `git diff origin/bmad-self-host..HEAD --stat` shows only `PLAN.md`. Amelia's next dispatch starts from this clean base.

4. **No origin-side recovery action needed.** Do not push the branch, do not re-create it on origin, do not interact with PR #17. Marcus owns publication at `in_review`. If Amelia's tooling or muscle memory expects `origin/<branch>` to exist (e.g. to validate the migration prefix), it should resolve `origin/bmad-self-host` instead — that is the actual base for prefix discovery.

## Out of scope

- Standalone `team-app/` repo, `mirror_*` tables, capacity service, gate handler — Epic 1 stories M-PR#2, M-PR#3 and beyond. This story stays inside `multica/server/...`.
- WS event emissions on `estimate_minutes` change — AC8 forbids behavioural drift outside the new field.
- `BatchUpdateIssues`, daemon handlers, search, autopilot, label enrichment — left untouched (AC8).
- A separate `<NNN+1>_team_app_ws_events.up.sql` placeholder migration — explicitly retired in spec §6.1 line 983.
- A `UNIQUE (id, workspace_id)` constraint on `issue` — explicitly retired in spec §6.1 NOTE.
- Frontend UI surfacing of `estimate_minutes` — Multica's v1 UI ignores the field; team-app reads it via M-PR#3.
- Running `ComputeIssueEstimateRollup` on list paths (`ListIssues`, `ListOpenIssues`, `ListChildIssues`) — single-issue computation only (perf).
- Adding `estimate_minutes` to `CreateIssueRequest` — v1 sets estimates post-creation via PUT; AC5 does not require it.
- Splitting the column add and the index create into two migration files — spec §6.1 collapses both DDLs into one transaction; splitting wastes a prefix slot and breaks the M-PR#1 contract.
- Pushing the branch to origin or interacting with PR #17 — Marcus owns publication at `in_review`.
