# PLAN — TIM-1: Story 1.1: Scaffold the standalone Go + Next.js application

> Generated by Winston (bmad-agent-architect) on 2026-04-27. The full
> cross-repo plan lives on the Multica issue. This file is the slice
> for THIS repo only.

## Branch
`agent/architect/848e501d` → base: `bmad-self-host`

## Files to touch (this repo)

| Path | Change | Why |
|---|---|---|
| `team-app/cmd/server/main.go` | new | AC4, AC5 — boot, env-var fail-fast, slog, pgxpool, `:8080` |
| `team-app/cmd/server/router.go` | new | AC4 — Chi router, `/healthz`, `/api/v1` placeholder |
| `team-app/cmd/server/main_test.go` | new | AC5 — env-var fail-fast unit tests |
| `team-app/internal/auth/doc.go` | new | AC3 — package skeleton (populated in Story 1.5) |
| `team-app/internal/events/doc.go` | new | AC3 — package skeleton (populated in Story 1.7+) |
| `team-app/internal/handler/doc.go` | new | AC3 — package skeleton + AR13 layering banner |
| `team-app/internal/handler/workspace_work_week.go` | new | AR14 — sole-writer marker for `mirror_workspace.work_week` |
| `team-app/internal/middleware/doc.go` | new | AC3 — package skeleton (populated in Story 1.5+) |
| `team-app/internal/multica/doc.go` | new | AR14 — sole-writer marker for `mirror_*` tables |
| `team-app/internal/service/anomaly/doc.go` | new | AC3 — package skeleton |
| `team-app/internal/service/approval/doc.go` | new | AR14 — sole-writer marker for `time_confirm.status`, `time_confirm_history` |
| `team-app/internal/service/autofill/doc.go` | new | AR14 — sole-writer marker for auto `time_entry` rows |
| `team-app/internal/service/capacity/doc.go` | new | AC3 — package skeleton (NFR39 100% later) |
| `team-app/internal/service/org/doc.go` | new | AC3 — package skeleton |
| `team-app/internal/service/rrule/doc.go` | new | AC3 — package skeleton (NFR40 95% later) |
| `team-app/pkg/db/queries/.gitkeep` | new | AC3 — empty dir survives git (1.6 lands queries) |
| `team-app/pkg/db/generated/.gitkeep` | new | AC3 — sqlc output dir |
| `team-app/migrations/.gitkeep` | new | AC3 — empty dir (1.6 lands `001_init.up.sql`) |
| `team-app/scripts/team-app-cli/main.go` | new | AC3 — placeholder (8.9 implements `org bootstrap`) |
| `team-app/sqlc.yaml` | new | AC3 — sqlc config mirroring `server/sqlc.yaml` |
| `team-app/Dockerfile` | new | AC4 — multi-stage Go build |
| `team-app/docker-compose.yml` | new | AC4 — db / api / frontend services |
| `team-app/nginx/team.conf` | new | AC4 — reverse-proxy `/api/`, `/gates/`, `/` |
| `team-app/.env.example` | new | AC5 — documents the nine boot vars |
| `team-app/.gitignore` | new | AC3 — ignores `.env`, `frontend/.next/`, `frontend/node_modules/` |
| `team-app/README.md` | new | Task 8.1 — run instructions + AR10 migration heads-up |
| `team-app/frontend/.gitkeep` | new | placeholder; dev runs `pnpm create next-app frontend …` (AC2) |
| `team-app/frontend/Dockerfile` | new | AC4 — Node 22 frontend image |
| `team-app/frontend/.eslintrc.cjs` | new | AC6, AR22 — `no-restricted-imports` boundaries |
| `team-app/frontend/.env.local.example` | new | AC4 — frontend env template |
| `team-app/frontend/packages/README.md` | new | Task 8.2 — boundary-rule documentation |
| `team-app/frontend/packages/core/team/index.ts` | new | AC6 — UI-free core package boundary marker |
| `team-app/frontend/packages/views/team/index.ts` | new | AC6 — Next-free views package boundary marker |

> **Note:** `.github/workflows/team-app-ci.yml` is intentionally omitted from this architect scaffold because the architect's OAuth token lacks GitHub `workflow` scope. The dev (Amelia) will author this file directly on `agent/dev/848e501d` — see Approach step 20 below for the spec.

## Approach (this repo)

1. Create `team-app/` at repo root (sibling of `server/`, `apps/`, `packages/`); confirm `git status` shows only additions under `team-app/` and `.github/workflows/team-app-ci.yml`.
2. Run `cd team-app && go mod init github.com/multica-ai/team-app` to init the Go module against Go 1.26.1 (matching `server/go.mod`); commits land in `team-app/go.mod` (AC1).
3. From `team-app/`, `go get` each pinned library from AC1 (`go-chi/chi/v5`, `jackc/pgx/v5`, `sqlc-dev/sqlc/cmd/sqlc`, `teambition/rrule-go`, `gorilla/websocket`, `google/uuid`, `prometheus/client_golang/prometheus`); run `go mod tidy`; commit `go.mod` + `go.sum` (AC1).
4. Replace `team-app/cmd/server/main.go` stub with the boot: load + validate the **nine** env vars from Dev Notes (`DEFAULT_ORG_SLUG`, `DEFAULT_ORG_NAME`, `ORG_CREATION_ENABLED`, `TEAM_APP_SHARED_SECRET`, `TEAM_APP_URL`, `MULTICA_BASE_URL`, `MULTICA_WS_URL`, `TEAM_APP_SYSTEM_USER_PAT`, `TEAM_APP_SYSTEM_USER_ID`); on any missing/empty/unparseable value, `slog.Error` with `missing_env_var=<NAME>` and `os.Exit(1)`. Connect via `pgxpool.New(ctx, dsn)`. Mount the Chi router from `router.go` and `http.ListenAndServe(":8080", r)`. Add a `// TIME-RULE:` comment near any `time.Now` use (AC5, AC4).
5. Replace `team-app/cmd/server/router.go` stub with a Chi router exposing `GET /healthz` (200 OK) and a placeholder `chi.Mux` group for `/api/v1`; add a header banner reminding contributors of AR13 layering (handlers thin → service → sqlc) (AC4).
6. Replace `team-app/cmd/server/main_test.go` stub with subtests that use `t.Setenv` to assert each missing env var causes the documented fail-fast (one subtest per variable; nine total). For `ORG_CREATION_ENABLED`, also assert that an unparseable value (e.g. `"yes"`) fails the same way (AC5).
7. Fill the per-package `doc.go` placeholders' sole-writer banners with the exact wording from Dev Notes AR14 (`approval`, `autofill`, `multica`, and `internal/handler/workspace_work_week.go`); leave the rest of the package skeleton untouched (AC3, AR14).
8. Author `team-app/sqlc.yaml` mirroring `server/sqlc.yaml`: `version: "2"`, `sql.engine: postgresql`, `sql.queries: pkg/db/queries`, `sql.schema: migrations`, `sql.gen.go.out: pkg/db/generated`, `sql.gen.go.sql_package: pgx/v5`, `emit_json_tags: true`, `emit_empty_slices: true`, `emit_db_tags: true` (AC3).
9. From `team-app/`, run the exact AC2 command `pnpm create next-app frontend --ts --app --tailwind --eslint --no-src-dir --import-alias "@/*"`. Delete the `team-app/frontend/.gitkeep` placeholder once the create-next-app output replaces it (AC2).
10. From `team-app/frontend/`, run `pnpm add @tanstack/react-query @base-ui/react zustand date-fns`; commit the updated `package.json` + `pnpm-lock.yaml` (AC2). Do NOT add `team-app/frontend` to root `pnpm-workspace.yaml` (Dev Notes Project Structure Notes).
11. Configure `team-app/frontend/app/globals.css` (or new `tokens.css`) with shadcn-style semantic CSS-variable tokens (`--background`, `--foreground`, `--destructive`, etc.); reference `packages/ui/styles/` shape but define independently (Task 3.3, CLAUDE.md "no hardcoded hex" rule).
12. Replace `team-app/frontend/packages/core/team/index.ts` stub with a placeholder export plus a banner comment encoding AR22 boundary rules (no `next/*`, no `react-router-dom`, no `react-dom`, no UI libs, no `process.env`, no `localStorage`/`window`); replace `team-app/frontend/packages/views/team/index.ts` with the matching banner (no `next/*`, no `react-router-dom`, no Zustand-store imports) (AC6, AR22).
13. Author `team-app/frontend/.eslintrc.cjs` extending `next/core-web-vitals` with two `overrides` blocks: `files: ['packages/core/team/**']` and `files: ['packages/views/team/**']`, each with a `no-restricted-imports.patterns` array per AR22. Add `no-restricted-globals` for `process` inside `core/team/**`. Verify locally by inserting an intentional violation and running `pnpm lint` — must error — then revert (AC6, AR22).
14. Replace `team-app/Dockerfile` stub with a multi-stage build: stage 1 `golang:1.26-alpine` runs `go build -o /team-app-server ./cmd/server`; stage 2 `gcr.io/distroless/static` copies the binary, `EXPOSE 8080`, `ENTRYPOINT ["/team-app-server"]` (Task 4.1).
15. Replace `team-app/frontend/Dockerfile` stub with `node:22-alpine`, `pnpm install --frozen-lockfile`, `pnpm build`, `EXPOSE 3000`, `CMD ["pnpm","start"]` (Task 4.2).
16. Replace `team-app/docker-compose.yml` stub with three services per Dev Notes: `db` (`pgvector/pgvector:pg17`, `POSTGRES_DB=team_app` distinct from Multica per AR4, exposes 5432 on internal network only), `api` (built from `./Dockerfile`, `env_file: .env`, `depends_on: [db]`, ports `8080:8080`), `frontend` (built from `./frontend/Dockerfile`, `env_file: frontend/.env.local`, ports `3000:3000`) (AC4, AR4).
17. Replace `team-app/nginx/team.conf` stub with the reverse-proxy config from Dev Notes "Nginx config — exact routing": `server_name team.multica.uittai.com`; ordered `location /api/` → `proxy_pass http://api:8080`, `location /gates/` → `proxy_pass http://api:8080`, `location /` → `proxy_pass http://frontend:3000`; pass `Host`, `X-Forwarded-For`, `X-Forwarded-Proto`. Add a `location /api/v1/orgs/.+/events` block (regex match) with `proxy_buffering off; proxy_read_timeout 1h;` for SSE. Listens `:80`; comment Let's Encrypt cert paths as `# TODO: production cert` (AC4, AR7).
18. Replace `team-app/.env.example` stub with all nine variables from Dev Notes Environment Variables table plus `DATABASE_URL=postgresql://postgres:postgres@db:5432/team_app?sslmode=disable`. Replace `team-app/.gitignore` stub to ignore `.env`, `frontend/.next/`, `frontend/node_modules/`, `*.log` (Task 4.5).
19. Replace `team-app/frontend/.env.local.example` stub with `NEXT_PUBLIC_API_BASE_URL=http://localhost:8080` (frontend-side analog only — never reads server-side env vars per AR22).
20. Author `.github/workflows/team-app-ci.yml` (NEW — not pre-stubbed; architect OAuth lacks `workflow` scope). Three jobs (AR9):
    - `team-app-backend`: `runs-on: ubuntu-latest`, services `db: pgvector/pgvector:pg17`, sets up Go 1.26.1, runs `cd team-app && go test ./...`.
    - `team-app-frontend`: Node 22 + pnpm, runs `cd team-app/frontend && pnpm install --frozen-lockfile && pnpm lint && pnpm build`.
    - `team-app-migrate-test`: placeholder no-op (`echo "migration runner lands in Story 1.6"`) — reserves the slot.
21. Replace `team-app/README.md` stub with: how to run `docker compose up` from `team-app/`; how to run `cd team-app && go test ./...`; how to run `cd team-app/frontend && pnpm dev`; a "Companion Multica fork PRs" section telling whoever lands Stories 1.2/1.4 to run `git log --oneline -- server/migrations/ | head` against `origin/main` at PR-open time and **never** assume migration prefix `057` (per Git Intelligence Summary — `feature/issue-tags` already reserves `101`); and a footnote that AR8 says "eight" but boot validates **nine** vars including `TEAM_APP_SYSTEM_USER_ID` (Task 8.1).
22. Replace `team-app/frontend/packages/README.md` stub with one paragraph documenting the AR22 boundary rules and pointing to the ESLint config that enforces them (Task 8.2).
23. Local validation gate before opening the PR: `cd team-app && go test ./...` (env-var subtests pass), `cd team-app/frontend && pnpm lint && pnpm build` (lint catches a planted violation in a throwaway commit, then revert), `docker compose up` from `team-app/` and `curl http://localhost:8080/healthz` returns 200 + `curl http://localhost:3000` returns the Next default page (AC4, AC5, AC6).

## Test strategy (this repo)

- **Unit (Go):** `team-app/cmd/server/main_test.go` — one subtest per env var (nine subtests) using `t.Setenv` to assert fail-fast; one extra subtest that an unparseable `ORG_CREATION_ENABLED` value also fails (AC5).
- **Integration:** `docker compose up` from `team-app/` then `curl http://localhost:8080/healthz` (200 OK) and `curl http://localhost:3000` (default Next page); confirm Postgres came up on `team_app` database, distinct from Multica's (AC4, AR4).
- **Lint as test:** plant an intentional violation in `frontend/packages/core/team/index.ts` (e.g. `import "next/router";`), run `pnpm lint`, confirm it errors, then revert (AC6).
- **Manual verification:** directory tree matches Dev Notes "Project structure — full target tree" (AC3); `team-app/go.mod` lists every AC1 library; `team-app/frontend/package.json` lists every AC2 library.
- **Coverage targets:** none in this story (Dev Notes "Testing standards").

## Out of scope (this repo)

- `001_init.up.sql` — Story 1.6.
- Auth bridge / PAT validation cache / `/api/auth/bridge` handler — Story 1.5.
- WS subscriber + reconciler — Stories 1.7 / 1.8.
- `GET /api/me` system-PAT identity-mismatch boot validation — Story 1.9.
- Migration runner that applies `*.up.sql` on boot — Story 1.6 (stub as `// TODO: Story 1.6` panic-on-call).
- Any business handler, service, or schema beyond package-skeleton + sole-writer comments.
- Multica fork PRs M-PR#1/#2/#3 — Stories 1.2/1.3/1.4. Do NOT modify `server/`, `apps/`, `packages/`, or any Multica-side code in this PR.
- Adding `team-app/frontend` to root `pnpm-workspace.yaml`.
- Production TLS / Let's Encrypt — placeholder TODO only (AR7).
- Coverage targets NFR39–NFR42 — land per epic.

---

🤖 Architect-scaffolded. Developer (Amelia) branches from this commit
into `agent/dev/848e501d` and replaces the empty stubs with real
implementation per the impl-plan comment on the Multica issue.
