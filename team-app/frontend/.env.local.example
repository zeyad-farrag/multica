# Frontend-side env template for the team-app standalone Next.js app.
# Copy to .env.local for local dev. Server-side env vars live in team-app/.env
# (the Go server is the only consumer); the frontend never reads them per AR22.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
